# agent-context-diff

Compute diffs between agent context states. Added, removed, repeated messages.

```typescript
import { diffContext } from "agent-context-diff";
diffContext(beforeState, afterState);
```

MIT
